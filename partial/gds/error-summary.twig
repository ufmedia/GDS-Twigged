
{% if errors is defined and errors is not empty %}
    <div class="govuk-error-summary" data-module="govuk-error-summary">
        <div role="alert">
            <h2 class="govuk-error-summary__title">
                There is a problem
            </h2>
            <div class="govuk-error-summary__body">
                <ul class="govuk-list govuk-error-summary__list">
                    {% for error in errors %}
                        <li>
                            <a href="{{ error.link | default('#') }}">{{ error.message }}</a>
                        </li>
                    {% endfor %}
                </ul>
            </div>
        </div>
    </div>
{% endif %}

{# Usage 
{% set errors = [
    { message: 'Enter your full name', link: '#error-fullname' },
    { message: 'The date your passport was issued must be in the past', link: '#error-passport-date' }
] %}

{% include 'partial/gds/error-summary.twig' with { 
    'errors': errors
} %}

#}